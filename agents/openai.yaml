interface:
  display_name: "unmachined"
  short_description: "Remove AI tells from writing and interfaces"
  default_prompt: "Use $unmachined to audit this draft for AI tells, fix concrete findings, and verify the result."
